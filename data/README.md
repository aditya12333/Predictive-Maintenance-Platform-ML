# Data directories

- `raw/`: immutable downloaded source data.
- `interim/`: parsed and validated data.
- `processed/`: model-ready datasets.
- `quarantine/`: records rejected during validation.

Do not modify files in `raw/` after download. Create a new version instead.
